{
  "name": "@pinpoint/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest --run"
  },
  "dependencies": {
    "@pinpoint/shared": "*",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.0.0"
  }
}
