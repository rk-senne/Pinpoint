{
  "name": "@pinpoint/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end Playwright suite for Pinpoint. Designed to run against a live stack (docker compose up) and to skip gracefully when the API is unreachable.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.8.3"
  }
}
